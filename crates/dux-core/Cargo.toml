[package]
name = "dux-core"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
chrono = "0.4.41"
rustix = { version = "1.1", features = ["termios", "process", "event", "stdio", "fs"] }
